local utils = require('user.configs.heirline.utils')
local colors = require('user.configs.heirline.colors')

return { provider = '󰞱 %l:%c', hl = { fg = colors.vibrant_green, bold = true }, utils.space(2) }
